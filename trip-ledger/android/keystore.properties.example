# Copy to keystore.properties and fill in your values.
# keystore.properties is gitignored - never commit real credentials.
#
# Generate keystore (one-time):
#   keytool -genkey -v -keystore trip-ledger-release.keystore -alias tripledger -keyalg RSA -keysize 2048 -validity 10000
#
storeFile=trip-ledger-release.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=tripledger
keyPassword=YOUR_KEY_PASSWORD
